pre-commit:
  parallel: true
  commands:
    lint:
      run: npx eslint {staged_files} --fix
      glob: '*.{ts}'
    format:
      run: npx prettier --write {staged_files}
      glob: '*.{ts,md,json}'
      stage_fixed: true
